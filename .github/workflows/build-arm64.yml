name: Build ARM64 Ragflow (development)

on:
  workflow_dispatch:
  push:
    branches: [ "development" ]  

jobs:
  build-ragflow-arm64:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU (ARM64)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # STEP 1 — Build ragflow_deps for ARM64 locally in runner
      - name: Build ragflow_deps (ARM64)
        run: |
          docker buildx build \
            --platform linux/arm64 \
            -f Dockerfile.deps \
            -t infiniflow/ragflow_deps:latest \
            . --load

      # STEP 2 — Build and push main ARM64 Ragflow image
      - name: Build & Push Ragflow ARM64 image
        run: |
          docker buildx build \
            --platform linux/arm64 \
            -f Dockerfile \
            -t eurodoc/medlibre-ragflow:arm64-dev \
            -t eurodoc/medlibre-ragflow:v0.22.0-arm64-dev \
            . --push
