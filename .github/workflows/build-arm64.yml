name: Build ARM64 Ragflow (development)

on:
  workflow_dispatch:
  push:
    branches: [ "development" ]

jobs:
  build-ragflow-arm64:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1) Prepare Python environment and download deps (nltk_data, huggingface.co, jar, debs, etc.)
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python deps for download_deps.py
        run: |
          python -m pip install --upgrade pip
          # Use requirements if it exists, otherwise install minimal deps
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          else
            pip install huggingface_hub nltk tiktoken requests
          fi

      - name: Run download_deps.py
        run: |
          python download_deps.py
          echo "After download_deps.py:"
          ls -R

      - name: Set up QEMU (ARM64)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 2) Build ragflow_deps for ARM64 so Dockerfile can COPY from those files
      - name: Build ragflow_deps (ARM64)
        run: |
          docker buildx build \
            --platform linux/arm64 \
            -f Dockerfile.deps \
            -t infiniflow/ragflow_deps:latest \
            . --load

      # 3) Build and push main Ragflow ARM64 image
      - name: Build & Push Ragflow ARM64 image
        run: |
          docker buildx build \
            --platform linux/arm64 \
            -f Dockerfile \
            -t eurodoc/medlibre-ragflow:arm64-dev \
            -t eurodoc/medlibre-ragflow:v0.22.0-arm64-dev \
            . --push
